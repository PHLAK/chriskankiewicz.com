os: linux
dist: focal
language: php

php:
  - 7.4
  - nightly

jobs:
  allow_failures:
    - php: nightly

cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.npm
    - vendor

services:
  - mysql

before_install:
  - nvm install 14.15
  - npm config set "@fortawesome:registry" https://npm.fontawesome.com/
  - npm config set "//npm.fontawesome.com/:_authToken" $FONT_AWESOME_TOKEN

install:
  - composer install
  - npm ci && npm run production --verbose

script:
  - vendor/bin/php-cs-fixer fix --diff --dry-run
  - vendor/bin/phpstan analyze
  - vendor/bin/phpunit --coverage-text
